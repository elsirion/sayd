[package]
name = "misaki-en"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "English G2P frontend for Kokoro TTS, ported from misaki"

[dependencies]
fst = "0.4"
regex = "1"

[build-dependencies]
fst = "0.4"
serde_json = "1"

# Test-only. The runtime crate must stay free of serde_json (Global Constraints).
[dev-dependencies]
serde_json = "1"
