[package]
name = "kokoro"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Kokoro-82M ONNX inference"

[dependencies]
ort = { version = "=2.0.0-rc.13", default-features = false, features = ["load-dynamic"] }
serde_json = "1"

[features]
models = []
