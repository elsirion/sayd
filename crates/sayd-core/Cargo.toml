[package]
name = "sayd-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "sayd engine: queue, chunking, cleanup, config"

[dependencies]
regex = "1"
serde = { version = "1", features = ["derive"] }
toml = "0.8"

[dev-dependencies]
tempfile = "3"
