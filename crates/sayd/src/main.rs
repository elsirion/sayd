// `main` is a stub until Task 11 wires the engine up to it, so nothing in
// `ring` is reachable from here yet -- only from its own tests. Silence
// dead_code for the whole module rather than pretend this is a real gap.
#[allow(dead_code)]
mod ring;

fn main() {}
